<div class="field">
<p class="control title-input"><%= form.text_field :name, class:'input', required: true %></p>
<script>
  fs = document.getElementById('project_document_name');
  fs.focus();
  fs.select();
</script>
</div>

<div class="field">
<p class="control single-rich"><%= form.rich_text_area :content %></p>
</div>

<div class="actions is-pulled-right">
  <a href="<%= project_project_document_path(person.shortname, project, project_document) %>" class="button">Cancelar</a> <%= form.submit 'Actualizar', class:'button is-success' %>
</div>