<table class="crud-table table is-fullwidth is-hoverable">
  <tr>
    <% @crud_fields.each do |f| %>
      <% next if f[:options][:hide] || f[:options][:hide_index] %>
      <th><%= f[:text] %></th>
    <% end %>
    <th class="crud-actions"></th>
  </tr>  
  <% count = 0 %>  
  <% @entries.each do |entry| %>
    <% count = count + 1 %>
    <tr>
      <% @crud_fields.each do |f| %>
        <% next if f[:options][:hide] || f[:options][:hide_index] %>
        <td class="crud-td" onclick="window.location = '<%= polymorphic_path(entry) %>'"><%= entry[f[:field]] %></td>
      <% end %>
      <td class="crud-actions">
        <a href="<%= edit_polymorphic_path(entry) %>"><span class="icon is-small"><i class="fas fa-edit"></i></span></a>
        <%= link_to polymorphic_path(entry), data: {:confirm => '¿Confirmar?'}, :method => :delete do %>
          <span class="icon is-small"><i class="fas fa-trash" aria-hidden="true"></i></span>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
<% if count == 0 %>
  <div class="empty-results">No existen</div>
<% end %>